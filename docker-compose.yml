version: '3.8'

services:
  app:
    build: .
    ports:
      - "5000:10000"
    env_file:
      - .env
    environment:
      - PORT=10000
      - NODE_ENV=production
      - CORS_ORIGIN=http://localhost:5000
    volumes:
      - ./backups:/app/backups
    restart: always
